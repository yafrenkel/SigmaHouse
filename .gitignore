# Real WiFi credentials — never commit (copy secrets_example.py -> secrets.py locally)
secrets.py

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.Python

# Pre-downloaded offline install bundle (large, machine-specific)
wheels/

# MicroPython firmware binaries (large; downloaded per-camp)
*.bin
firmware/

# Editor / OS noise
.idea/
.vscode/
.DS_Store
Thumbs.db

# draw.io backups
*.bkp
*.dtmp
